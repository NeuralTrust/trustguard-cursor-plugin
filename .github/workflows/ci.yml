# Tests and vets the hook binary sources on every PR and push to main.

name: CI

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    name: Test (go)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Vet
        run: go vet ./cli/

      - name: Test
        run: go test -race ./cli/

      - name: Bootstrap script syntax
        run: |
          sh -n trustguard/hooks/trustguard-hook.sh
          sh trustguard/hooks/trustguard-hook.cmd   # polyglot: must be a silent no-op under sh
